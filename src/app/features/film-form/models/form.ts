export interface Form {
  countryGroup: {
    contry: string;
  };
  favouriteMovie: string;
  firstName: string;
  lastName: string;
  postCode: string;
  titleGroup: {
    title: string;
  };
  username: string;
}
